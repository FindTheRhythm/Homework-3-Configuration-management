/# Это многострочный комментарий
Он содержит несколько строк текста,
которые игнорируются парсером #/

/# Определяем константы#/

(define MAX_SIZE 10);
(define OFFSET 5);
(define NEGATIVE_NUM -10);

/# Вычисляем выражение с использованием констант в постфиксной нотации#/

@(MAX_SIZE OFFSET +)

@(NEGATIVE_NUM abs)

@(
OFFSET 1 + NEGATIVE_NUM + abs
)

/# Определяем массив с числами#/
({ 1, 2, 3, 4 })

/# Используем массив с константами и числами#/

({
MAX_SIZE, OFFSET, 100, NEGATIVE_NUM
})
